---
import { Code } from 'astro:components'
const { language = 'javascript', children } = Astro.props

// Ensure code is properly formatted
const codeContent = String(children).trim()
---

<div class="my-8">
  <Code lang={language} wrap theme="github-dark">
    {codeContent}
  </Code>
</div>
